import InfinityList from "./src/InfinityList.vue";

// 如果想通过CDN方式引入，需要编写install函数
// 注册组件
// tButton.install = function (Vue) {
//     Vue.component(tButton.name, tButton);
// };

export default InfinityList;